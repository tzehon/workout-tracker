# ==============================================================================
# Environment Variables for Workout Tracker
# ==============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.local.example .env.local
#
# NEVER commit .env.local to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# MongoDB Connection
# ------------------------------------------------------------------------------
# For local development (Atlas CLI):
#   1. Install Atlas CLI: brew install mongodb-atlas-cli
#   2. Create local deployment: atlas deployments setup local --type local
#   3. Start it: atlas deployments start local
#   4. Get your connection string: atlas deployments connect local --connectWith connectionString
#   5. Append the database name before query params (see example below)
#
# Format: mongodb://localhost:<port>/workout-tracker?directConnection=true
# Example: If atlas gives you mongodb://localhost:51120/?directConnection=true
#          Change it to:    mongodb://localhost:51120/workout-tracker?directConnection=true
MONGODB_URI=mongodb://localhost:51120/workout-tracker?directConnection=true

# For Atlas Cloud (staging/production), use your cluster connection string:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/workout-tracker?retryWrites=true&w=majority

# ------------------------------------------------------------------------------
# Google OAuth Credentials
# ------------------------------------------------------------------------------
# Get these from Google Cloud Console:
#   1. Go to https://console.cloud.google.com/
#   2. Create a project (e.g., "workout-tracker-dev")
#   3. Go to APIs & Services > Credentials
#   4. Create OAuth 2.0 Client ID (Web application)
#   5. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
#
# For staging (Vercel previews), also add:
#   https://*.vercel.app/api/auth/callback/google
#
# See README.md for detailed setup instructions.
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ------------------------------------------------------------------------------
# NextAuth.js Configuration
# ------------------------------------------------------------------------------
# Generate a secure secret with:
#   openssl rand -base64 32
#
# Use a DIFFERENT secret for each environment (local, staging, production)!
NEXTAUTH_SECRET=your-random-secret-key-here

# The canonical URL of your site
# - Local development: http://localhost:3000
# - Production: https://workout.tth.dev
# - Staging/Preview: Leave unset (Vercel auto-detects)
NEXTAUTH_URL=http://localhost:3000
